class Solution(object):
    def multiply(self, num1, num2):
        a = 0

        for i in num1:
            a = a * 10 + int(i)

        b = 0

        for i in num2:
            b = b * 10 + int(i)

        c = a * b

        return str(c)