class Solution(object):
    def jump(self, nums):
        maxreach=0
        jump=0
        end=0
        for i in range(len(nums)-1):
            maxreach=max(maxreach,i+nums[i])
            if i==end:
                jump+=1
                end=maxreach
        return jump